import { GravatarSourcePipe } from './gravatar-source.pipe';

describe('GravatarSourcePipe', () => {
  it('create an instance', () => {
    const pipe = new GravatarSourcePipe();
    expect(pipe).toBeTruthy();
  });
});
